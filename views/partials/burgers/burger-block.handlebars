<p>
	{{burger_name}}

	<button class="change-devoured btn btn-danger btn-sm" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if checkdevoured}}Digest{{else}}Nom nom{{/if}}
	</button>

</p>


<script>
$(".change-devoured").on("click", function(event){
  event.preventDefault();
  if ($(this).data("newdevoured") == false){
    var burgerName = $(this).attr("data-id");
  $.ajax({
    method: "PUT",
    url: "/api/burgers/" + burgerName,
  }).then(function(result, err){
    location.reload();
  })} else {
    var burgerName = $(this).attr("data-id");
  $.ajax({
    method: "DELETE",
    url: "/api/burgers/" + burgerName,
  }).then(function(result, err){
    location.reload();
  })}
});

</script>

